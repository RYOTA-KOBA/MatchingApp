import React, { useState } from "react";

export const sample = () => {
  return (
    <>
      <p>Learn React</p>
    </>
  );
};

export const multiple = (x, y) => x * y;
export const add = (x, y) => x + y;
