version: 2
jobs:
  deploy:
    docker:
      - image: circleci/node:lts-browsers
    steps:
      - checkout
      - run: npm install
      - run: npm run build
      - run: gcloud -q app deploy
workflows:
  version: 2
  deploy:
    jobs:
      - deploy
