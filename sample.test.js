describe("Jest動作チェック", () => {
    test("サンプルテスト", async () => {
        expect(3 + 7).toBe(10);
    });
});
describe("Jest動作チェック", () => {
    test("サンプルテスト", async () => {
        expect(5 + 5).toBe(10);
    });
});